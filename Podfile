platform :ios, '8.0'

target 'HKNSLayoutConstraint' do

  # uncomment the following line to use frameworks, leave it commented to use static libraries

  use_frameworks!
  pod 'HKDeviceUtilities', '~> 1.0.0'
  pod 'HKUIViewLengthCalculationBasis', '~> 1.0.0'

end
